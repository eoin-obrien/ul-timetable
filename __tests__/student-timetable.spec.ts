import { studentTimetable } from '../src';

const studentTimetableURI = 'https://www.timetable.ul.ie/tt2.asp';

describe('studentTimetable()', () => {
  it('requests and parses the student timetable');

  it('throws an error if the student ID is invalid');

  it('throws an error if the request fails');
});
