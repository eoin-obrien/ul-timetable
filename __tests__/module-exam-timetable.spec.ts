import { moduleExamTimetable } from '../src';

const moduleExamTimetableURI = 'https://www.timetable.ul.ie/mod_exam_res.asp';

describe('moduleExamTimetable()', () => {
  it('requests and parses the module exam timetable');

  it('throws an error if the module ID is invalid');

  it('throws an error if the request fails');
});
