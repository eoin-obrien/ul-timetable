import { roomDetails } from '../src';

const roomDetailsURI = 'https://www.timetable.ul.ie/room_res.asp';

describe('roomDetails()', () => {
  it('parses the room details from the room ID');

  it('throws an error if the room ID is invalid');

  it('throws an error if the request fails');
});
