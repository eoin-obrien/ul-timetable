import { weekDates } from '../src';

const weekDatesURI = 'https://www.timetable.ul.ie/weeks.htm';

describe('weekDates()', () => {
  it('requests and parses the week dates');

  it('throws an error if the request fails');
});
