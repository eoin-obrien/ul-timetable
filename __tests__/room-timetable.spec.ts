import { roomTimetable } from '../src';

const roomTimetableURI = 'https://www.timetable.ul.ie/room_res.asp';

describe('roomTimetable()', () => {
  it('requests and parses the room timetable');

  it('throws an error if the room ID is invalid');

  it('throws an error if the request fails');
});
