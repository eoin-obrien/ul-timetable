import { courseTimetable } from '../src';

const courseTimetableURI = 'https://www.timetable.ul.ie/course_res.asp';

describe('courseTimetable()', () => {
  it('requests and parses the course timetable');

  it('throws an error if the course ID is invalid');

  it('throws an error if the year is invalid');

  it('throws an error if the request fails');
});
