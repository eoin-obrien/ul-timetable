import { moduleTimetable } from '../src';

const moduleTimetableURI = 'https://www.timetable.ul.ie/mod_res.asp';

describe('moduleTimetable()', () => {
  it('requests and parses the module timetable');

  it('throws an error if the module ID is invalid');

  it('throws an error if the request fails');
});
