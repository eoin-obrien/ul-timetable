import { moduleDetails } from '../src';

const moduleDetailsURI = 'https://www.timetable.ul.ie/tt_moduledetails_res.asp';

describe('moduleDetails()', () => {
  it('requests and parses the module details');

  it('throws an error if the module ID is invalid');

  it('throws an error if the request fails');
});
